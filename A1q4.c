/*WAP to print the area of the circle Take raduis of circle from user as input and priint the result in below given format.*/
#include"stdio.h"
int main()
{
float R,A;
printf("Enter raduis of circle : ");
scanf("%f",&R);
printf("area of circle is %f    %f",22.7*R*R);
    return 0;
}